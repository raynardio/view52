<div class="web-link">
  <div class="card">
    <div class="card-body">
      <div class="card-title row">
        <img class="favicon col-md-2" src="data:<%= @link.favicon.content_type %>;base64, <%= @link.favicon.base64 %>" />
        <a class="col-md" href="<%= @link.url %>"><%= @link[:title] %></a>
      </div>
      <div class="card-text"><%= @link[:description] %></div>
    </div>
  </div>
</div>
